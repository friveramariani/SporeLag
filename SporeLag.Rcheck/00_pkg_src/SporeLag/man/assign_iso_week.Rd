% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_iso_week.R
\name{assign_iso_week}
\alias{assign_iso_week}
\title{Assign ISO 8601 week and year}
\usage{
assign_iso_week(data, date, week_col = "iso_week", year_col = "iso_year")
}
\arguments{
\item{data}{A \code{data.frame}.}

\item{date}{A single string giving the name of a \code{Date} column in \code{data}.}

\item{week_col}{Name of the ISO week column to append. Default \code{"iso_week"}.}

\item{year_col}{Name of the ISO year column to append. Default \code{"iso_year"}.}
}
\value{
\code{data} with two integer columns appended: \code{week_col} (1–53) and
\code{year_col}. Row order and all input columns are unchanged.
}
\description{
\code{assign_iso_week()} appends the ISO 8601 week number and ISO year for each
date. It is a row-wise mapping: no grouping, no temporal indexing, no
requirement that the grid be complete.
}
\details{
\strong{The ISO year is not the calendar year, and this matters.} Late-December
and early-January dates routinely belong to an ISO week owned by the
adjacent year:

\tabular{lll}{
\strong{Date}     \tab \strong{Weekday} \tab \strong{ISO} \cr
2019-12-30   \tab Monday      \tab 2020-W01 \cr
2020-12-31   \tab Thursday    \tab 2020-W53 \cr
2021-01-01   \tab Friday      \tab 2020-W53 \cr
2023-01-01   \tab Sunday      \tab 2022-W52 \cr
}

Consequently, \strong{week number alone is not a valid grouping key.} Grouping a
multi-year series by \code{iso_week} pools week 1 of every year together, and
pools the days either side of a New Year boundary into different weeks than
the calendar suggests. Always group by \code{c(iso_year, iso_week)}. Downstream
aggregation functions in SporeLag require both columns for this reason.

ISO weeks are computed in base R via the nearest-Thursday rule rather than
\code{strftime()}'s \verb{\%V}/\verb{\%G} format codes, whose behaviour has historically
varied by platform. Output is therefore identical on every operating system.
}
\section{Errors}{

\describe{
\item{\code{sporelag_error_bad_input}}{\code{data}, \code{date}, \code{week_col}, or
\code{year_col} do not match the documented types; \code{date} is not a \code{Date}
column; or an output column name already exists in \code{data}.}
\item{\code{sporelag_error_missing_date}}{\code{date} contains \code{NA}.}
}
}

\examples{
df <- data.frame(
  date = as.Date(c("2019-12-30", "2020-12-31", "2021-01-01")),
  count = c(1, 2, 3)
)
assign_iso_week(df, date = "date")

}
\seealso{
\code{\link[=assign_season]{assign_season()}}
}
