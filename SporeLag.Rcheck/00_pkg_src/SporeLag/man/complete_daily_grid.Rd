% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/complete_daily_grid.R
\name{complete_daily_grid}
\alias{complete_daily_grid}
\title{Build a complete daily grid}
\usage{
complete_daily_grid(data, date, by = character())
}
\arguments{
\item{data}{A \code{data.frame}.}

\item{date}{A single string giving the name of a \code{Date} column in \code{data}.}

\item{by}{A character vector of column names in \code{data} to group by.
Defaults to \code{character()}, meaning no grouping (the whole \code{data.frame}
is treated as a single daily series). Gaps are filled, and dates are
checked for duplicates, independently within each group.}
}
\value{
A \code{data.frame} with the same columns as \code{data}, in the same
order, with one row per calendar day between \code{min(date)} and
\code{max(date)} within each group. Rows are sorted by \code{by}, then \code{date}.
No columns are added or removed; this function only inserts rows.
}
\description{
\code{complete_daily_grid()} fills gaps in a daily time series so that every
calendar day between the minimum and maximum date (within each group) is
represented by exactly one row. Newly inserted rows carry \code{NA} for every
column except \code{date} and the columns named in \code{by}.
}
\details{
Functions in SporeLag that index by time (lags, moving averages) assume
the day-to-day step is exactly one day with no repeats and no gaps within
a group. Rather than silently shifting values by row position when that
assumption fails, those functions raise a classed error. This function is
the documented mechanism for producing that guarantee before calling them.

\code{complete_daily_grid()} never guesses at missing values: inserted rows
get \code{NA}, and it is the caller's responsibility to decide whether and how
to impute them.
}
\section{Errors}{

\describe{
\item{\code{sporelag_error_bad_input}}{\code{data}, \code{date}, or \code{by} do not match
the documented types, or name columns not present in \code{data}.}
\item{\code{sporelag_error_duplicate_dates}}{The same date appears more than
once within a single group. A grid cannot be built unambiguously, so
the function errors instead of silently choosing a row.}
}
}

\examples{
df <- data.frame(
  site = c("A", "A", "A", "B", "B"),
  date = as.Date(c(
    "2024-01-01", "2024-01-02", "2024-01-04",
    "2024-01-01", "2024-01-03"
  )),
  count = c(1, 2, 4, 10, 30)
)
complete_daily_grid(df, date = "date", by = "site")

}
