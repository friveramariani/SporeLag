% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_lag.R
\name{apply_lag}
\alias{apply_lag}
\title{Apply time-indexed lags to an exposure series}
\usage{
apply_lag(data, value, lags, date, by = character())
}
\arguments{
\item{data}{A \code{data.frame} on a complete daily grid.}

\item{value}{A single string naming a numeric exposure column.}

\item{lags}{A vector of non-negative whole numbers, e.g. \code{0:3}.}

\item{date}{A single string naming a \code{Date} column.}

\item{by}{A character vector of grouping column names. Default
\code{character()} (no grouping — the whole frame is one series).}
}
\value{
\code{data} with one column appended per lag, named \verb{\{value\}_lag\{n\}}.
Row order and all input columns are unchanged.
}
\description{
\code{apply_lag()} appends one column per requested lag, shifting the exposure
series backwards in time within each group. A lag of \code{n} places the value
observed \code{n} days earlier alongside the current day.
}
\details{
\strong{This function indexes by position, and therefore requires a complete daily
grid.} On a gapped series, "lag 1" would mean "the previous \emph{row}", which
may be one day earlier or thirty. The resulting exposure column looks
entirely plausible, runs cleanly through a regression, and yields a
silently misaligned lag–response estimate. \code{apply_lag()} therefore refuses
to operate on a gapped grid and raises \code{sporelag_error_gaps}. Call
\code{\link[=complete_daily_grid]{complete_daily_grid()}} first. This is a deliberate design decision, not an
oversight; see \code{vignette("getting-started")}.

\strong{Documented defaults:}
\itemize{
\item Lags are computed strictly \strong{within} each group defined by \code{by}. Values
never carry across a group boundary — the first \code{n} days of each site's
series are \code{NA}, not the tail of the previous site's series.
\item \code{lags = 0} is permitted and yields a copy of the same-day value. This
exists so a uniform model matrix (\verb{lag0, lag1, lag2, ...}) can be built
without special-casing the contemporaneous term.
\item \strong{Negative values are an error.} A negative lag is a \emph{lead}: it aligns
an exposure with an outcome that preceded it. That is almost never
intended, and when it is (e.g. a negative-control exposure), it should be
written explicitly rather than arrived at by a sign slip.
\item Row order is preserved. Columns are appended.
}
}
\section{Errors}{

\describe{
\item{\code{sporelag_error_gaps}}{The daily grid is incomplete within at least
one group. Call \code{\link[=complete_daily_grid]{complete_daily_grid()}} first.}
\item{\code{sporelag_error_duplicate_dates}}{A date appears more than once
within a group.}
\item{\code{sporelag_error_bad_input}}{Bad types; missing columns; \code{value} is
not numeric; \code{lags} contains negative or non-whole numbers; an output
column name already exists.}
}
}

\examples{
df <- data.frame(
  site  = rep(c("A", "B"), each = 4),
  date  = rep(seq(as.Date("2024-05-01"), by = "day", length.out = 4), 2),
  count = c(10, 20, 30, 40, 100, 200, 300, 400)
)

apply_lag(df, value = "count", lags = 0:2, date = "date", by = "site")

}
\seealso{
\code{\link[=complete_daily_grid]{complete_daily_grid()}}, \code{\link[=build_moving_average]{build_moving_average()}}
}
