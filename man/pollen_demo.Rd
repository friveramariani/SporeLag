% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{pollen_demo}
\alias{pollen_demo}
\title{Synthetic daily pollen counts from two monitoring sites}
\format{
A data frame with three columns:
\describe{
\item{site}{\code{character}. Monitoring station: \code{"North"} or \code{"South"}.}
\item{date}{\code{Date}. Calendar day, spanning 2024-02-15 to 2024-06-30.}
\item{count}{\code{integer}. Pollen concentration in grains per cubic metre.
\code{NA} where sampling failed on a day that was otherwise monitored.}
}
}
\source{
Simulated. \strong{These are not real surveillance data} and must not be
used for any substantive inference about pollen exposure. See
\code{data-raw/make_pollen_demo.R} for the generating code.
}
\usage{
pollen_demo
}
\description{
A \strong{synthetic} dataset of daily airborne pollen concentrations for a single
spring tree-pollen season at two hypothetical monitoring stations. It exists
to demonstrate and test SporeLag and is used throughout
\code{vignette("getting-started")}.
}
\details{
The data deliberately contain \strong{both} of the defects SporeLag is designed
to handle, because they are different problems and are frequently confused:

\describe{
\item{Gaps}{Calendar days with \strong{no row at all}, representing periods
when the sampler was offline: 2024-03-18 to 2024-03-22 at North, and
2024-04-01 to 2024-04-03 at South. A gap is invisible to \code{is.na()} — the
row simply is not there. Gaps must be closed with
\code{\link[=complete_daily_grid]{complete_daily_grid()}} before any lag or moving average is computed, or
the shift will be by row position rather than by time.}
\item{Missing values}{Days that \strong{are} present but whose \code{count} is \code{NA},
representing failed samples on otherwise-monitored days (roughly 6\% of
days at each site). These are candidates for \code{\link[=impute_weekly_mean]{impute_weekly_mean()}}.}
}

The South gap falls at that site's seasonal peak — the point at which losing
data does the most damage to an exposure estimate, and where a naive
positional lag would silently produce the largest misalignment.

Counts were drawn from a negative binomial distribution around a Gaussian
seasonal curve, so they are overdispersed in the way real aeroallergen counts
are. The generating script is \code{data-raw/make_pollen_demo.R} and is fully
deterministic.
}
\examples{
str(pollen_demo)

# Gaps are absent rows, not NA values -- is.na() cannot see them:
table(pollen_demo$site)          # neither site has a full 137 days
sum(is.na(pollen_demo$count))    # the NAs are a separate problem

# The canonical pipeline:
pollen_demo |>
  complete_daily_grid(date = "date", by = "site") |>
  assign_iso_week(date = "date") |>
  impute_weekly_mean(value = "count", by = "site") |>
  apply_lag(value = "count_imputed", lags = 0:2, date = "date", by = "site") |>
  head()

}
\seealso{
\code{\link[=complete_daily_grid]{complete_daily_grid()}}, \code{\link[=impute_weekly_mean]{impute_weekly_mean()}}
}
\keyword{datasets}
