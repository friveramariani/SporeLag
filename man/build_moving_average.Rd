% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_moving_average.R
\name{build_moving_average}
\alias{build_moving_average}
\title{Build moving-average exposure windows}
\usage{
build_moving_average(
  data,
  value,
  window,
  date,
  by = character(),
  align = c("right", "center"),
  min_obs = window
)
}
\arguments{
\item{data}{A \code{data.frame} on a complete daily grid.}

\item{value}{A single string naming a numeric exposure column.}

\item{window}{A vector of positive whole numbers, e.g. \code{c(3, 7)}.}

\item{date}{A single string naming a \code{Date} column.}

\item{by}{A character vector of grouping column names. Default \code{character()}.}

\item{align}{\code{"right"} (default, trailing) or \code{"center"}. See Details.}

\item{min_obs}{Minimum non-missing observations required within a window.
Defaults to \code{window} (no missingness tolerated). Recycled if \code{window} has
length > 1.}
}
\value{
\code{data} with one \code{double} column appended per window, named
\verb{\{value\}_ma\{k\}}. Row order and all input columns are unchanged.
}
\description{
\code{build_moving_average()} appends one column per requested window, containing
the mean exposure over that window, computed within each group.
}
\details{
Like \code{\link[=apply_lag]{apply_lag()}}, this function indexes by position and therefore
\strong{requires a complete daily grid}; it raises \code{sporelag_error_gaps}
otherwise. Call \code{\link[=complete_daily_grid]{complete_daily_grid()}} first.

\strong{Documented defaults — each of these changes what the variable means:}

\describe{
\item{\code{align = "right"} (default)}{The window is \strong{trailing} and
\strong{inclusive of the current day}: \code{window = 3} averages today and the
two preceding days. This is the alignment appropriate for
exposure→outcome inference.}
\item{\code{align = "center"}}{The window is symmetric around the current day
and therefore \strong{incorporates future exposure}. If the outcome is
measured on the current day, a centred window conditions on information
that did not exist at the time — it can induce apparent associations
with no causal interpretation. It is provided for smoothing and
descriptive display, not for exposure construction. Requires an odd
\code{window}.}
\item{Edge windows are \code{NA}}{A window that extends past the start (or,
when centred, either end) of a group's series is incomplete by
construction and returns \code{NA}. The first \code{window - 1} days of each
trailing series are \code{NA}. They are not back-filled with a shorter
average, because a "7-day mean" computed from 2 days is not a 7-day
mean.}
\item{\code{min_obs = window} (default)}{The minimum number of \strong{non-missing}
observations required \emph{within} an otherwise complete window. At the
default, any \code{NA} in the window yields \code{NA}. Lowering it (e.g.
\code{min_obs = 5} with \code{window = 7}) tolerates missing days and averages
over those present — which shrinks variance and can bias a
lag–response estimate toward the null. Lower it deliberately, and
report that you did.}
}
}
\section{Errors}{

\describe{
\item{\code{sporelag_error_gaps}}{The daily grid is incomplete within at least
one group.}
\item{\code{sporelag_error_bad_input}}{Bad types; \code{value} is not numeric;
\code{window} is not a positive whole number; \code{min_obs} exceeds \code{window};
\code{align = "center"} with an even \code{window}; an output column already
exists.}
}
}

\examples{
df <- data.frame(
  site  = rep(c("A", "B"), each = 5),
  date  = rep(seq(as.Date("2024-05-01"), by = "day", length.out = 5), 2),
  count = c(10, 20, 30, 40, 50, 100, 200, 300, 400, 500)
)

build_moving_average(df, value = "count", window = 3, date = "date",
                     by = "site")

}
\seealso{
\code{\link[=complete_daily_grid]{complete_daily_grid()}}, \code{\link[=apply_lag]{apply_lag()}}
}
